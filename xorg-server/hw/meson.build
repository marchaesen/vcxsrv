if get_option('xephyr')
    subdir('kdrive')
endif

if get_option('dmx')
    subdir('dmx')
endif

subdir('vfb')

if build_xnest
    subdir('xnest')
endif

if build_xorg
    subdir('xfree86')
endif

if build_xquartz
    subdir('xquartz')
endif

if build_xwayland
    subdir('xwayland')
endif
